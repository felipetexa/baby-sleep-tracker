
export const startTimer = () => {
  // Make API call to start timer on the backend
  console.log('Timer started on the backend');
};

export const pauseTimer = () => {
  // Make API call to pause timer on the backend
  console.log('Timer paused on the backend');
};

export const stopTimer = () => {
  // Make API call to stop timer on the backend
  console.log('Timer stopped on the backend');
};
